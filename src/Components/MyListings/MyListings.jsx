import React, { use } from 'react';
import { AuthContext } from '../../Contexts/AuthContext';

const MyListings = () => {

    return (
        <div>
            MyListings 
        </div>
    );
};

export default MyListings;